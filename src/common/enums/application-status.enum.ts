export enum ApplicationStatus {
  // Mentee pipeline
  APPLIED = 'applied',
  SCREENED = 'screened',
  APPROVED = 'approved',
  ENROLLED = 'enrolled',
  GRADUATED = 'graduated',
  // Mentor pipeline
  PENDING_APPROVAL = 'pending_approval',
  REJECTED = 'rejected',
}

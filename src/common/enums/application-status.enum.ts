export enum ApplicationStatus {
  // Mentee pipeline
  APPLIED = 'applied',
  SCREENED = 'screened',
  APPROVED = 'approved',
  ENROLLED = 'enrolled',
  // Mentor pipeline
  PENDING_APPROVAL = 'pending_approval',
  REJECTED = 'rejected',
}
